# Analyzesa/docs

Personal fork of github/docs for experiments.

## CLI Calculator

    node calculator.js 20 / 4     # => 5

Supported operators: + - * /

## Tests

    node --test

CI runs automatically via GitHub Actions on every push and PR.
